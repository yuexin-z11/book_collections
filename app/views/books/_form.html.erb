<%= form_with(model: @book, local: true) do |form| %>
  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>


  <p>
    <%= form.submit %>
  </p>
<% end %>

<%= link_to "Back to Home", root_path %>
